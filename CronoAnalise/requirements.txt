# Núcleo — roda em qualquer PC, sem GPU
fastapi>=0.110
uvicorn[standard]>=0.27
opencv-python-headless>=4.9
numpy>=1.26
python-multipart>=0.0.9
# Módulo do celular: certificado HTTPS local (libera a câmera) e QR de pareamento
cryptography>=42.0
qrcode>=7.4
