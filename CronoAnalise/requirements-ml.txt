# Opcional — detecção por objeto (YOLO) e por pose do operador.
# Só instale se o posto exigir: o detector padrão (movimento/presença) já resolve
# a maioria dos casos e roda em CPU fraca.
-r requirements.txt
ultralytics>=8.3
supervision>=0.22
mediapipe>=0.10
